import bcrypt from 'bcryptjs';import pg from 'pg';
const {Pool}=pg;const email=process.env.BOOTSTRAP_ADMIN_EMAIL?.trim().toLowerCase();const password=process.env.BOOTSTRAP_ADMIN_PASSWORD;const name=process.env.BOOTSTRAP_ADMIN_NAME||'Sanatan Gyan Admin';if(!email||!password||password.length<14)throw new Error('Set BOOTSTRAP_ADMIN_EMAIL and a 14+ character BOOTSTRAP_ADMIN_PASSWORD');const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.DATABASE_SSL==='true'?{rejectUnauthorized:false}:undefined});const hash=await bcrypt.hash(password,12);await pool.query(`INSERT INTO users(email,name,password_hash,role) VALUES($1,$2,$3,'SUPER_ADMIN') ON CONFLICT(email) DO UPDATE SET name=EXCLUDED.name,password_hash=EXCLUDED.password_hash,role='SUPER_ADMIN',is_active=true`,[email,name,hash]);await pool.end();console.log('SUPER_ADMIN created/updated:',email);
